pipeline {
    agent any

stages{	
      stage('build') {
            steps {
                echo 'build'      
	        }
                    }
      stage('Push') {
            steps {
              
                echo 'Prod'
                   
	    }
        }
                   
}
}
